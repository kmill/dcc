//This is from fig 16.1 of Muchnick (register allocation)
//>x=6 z=3 u=8
class Program {
  void main() {
    int x, y, z, w, u;
    x = 2;
    y = 4;
    w = x + y; // 6
    z = x + 1; // 3
    u = x * y; // 8
    x = z * 2; // 6
    callout("printf", "x=%d z=%d u=%d\n", x, z, u);
  }
}
